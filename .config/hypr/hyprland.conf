# 默认应用配置

# 终端模拟器
$terminal = foot
# 文件管理器
$fileManager = dolphin
# 应用启动噐
$menu = rofi -show drun
# 锁屏命令
$lockCommand = hyprlock
# 通过终端从输入法获取字符串命令
$getString = foot --app-id get-string vim -u ~/.vim/get-string.vimrc && wl-paste --no-newline | wtype -

# 自启动进程
# 输入法
exec-once = fcitx5 --replace -d
# 状态栏
exec-once = waybar -c ~/.config/waybar/config-hypr
# 壁纸
exec-once = swaybg -i ~/.config/wallpapers/wallpaper.png -m fill
# 定时锁屏
exec-once = hypridle -c ~/.config/hypridle/hypridle.conf
# 通知
exec-once = mako

# 环境变量
env = HYPRCURSOR_SIZE,24

# 视觉效果

general {
    # 窗口间距
    gaps_in = 2
    gaps_out = 5

    # 边框尺寸
    border_size = 1

    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    resize_on_border = false

    allow_tearing = false

    layout = dwindle
}

decoration {
    rounding = 5
    rounding_power = 2

    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

animations {
    enabled = yes, please :)

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
    animation = zoomFactor, 1, 7, quick
}

dwindle {
    pseudotile = true
    preserve_split = true
}

master {
    new_status = master
}

misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
}


# 输入配置

input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0

    touchpad {
        natural_scroll = true # 开启自然滚动
    }
}

device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


# 按键映射

# MainMod key
$mainMod = SUPER

# 打开终端
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, Q, exec, $terminal

# 通过终端从输入法获取字符串
bind = $mainMod, A, exec, $getString

# 关闭窗口
bind = $mainMod, C, killactive,

# 退出 hyprland 会话
bind = $mainMod, M, exit,

# 打开文件管理器
bind = $mainMod, E, exec, $fileManager

# 切换窗口浮动
bind = $mainMod, V, togglefloating,

# 应用启动噐
bind = $mainMod, D, exec, $menu

bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, Y, togglesplit, # dwindle

# 锁定桌面
bind = $mainMod SHIFT, Q, exec, $lockCommand

# 窗口全屏
bind = $mainMod, F, fullscreen

# 移动窗口焦点
bind = $mainMod, H, movefocus, l
bind = $mainMod, J, movefocus, d
bind = $mainMod, K, movefocus, u
bind = $mainMod, L, movefocus, r

# 切换工作区
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# 移动当前窗口到指定工作区
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# 暂存区
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# 窗口和工作区

windowrule = suppressevent maximize, class:.*
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

# get-string 窗口浮动
windowrulev2 = float, class:^get-string$

# JebBrain IDE 浮动窗口相关设置
windowrulev2 = tag +jb, class:^jetbrains-.+$,floating:1,title:win.*
windowrulev2 = noinitialfocus, tag:jb

# 显示器配置

#monitor=eDP-1,2880x1800,0x0,auto
#monitor=DP-2,1920x1080,-1920x-180,auto
#
#xwayland {
#    force_zero_scaling = true
#}

# 工作区配置
#workspace = 1, monitor:DP-2, default:true
#workspace = 2, monitor:DP-2
#workspace = 3, monitor:DP-2
#workspace = 4, monitor:DP-2
#workspace = 5, monitor:DP-2
#workspace = 5, monitor:eDP-1, default:true
#workspace = 6, monitor:eDP-1
#workspace = 7, monitor:eDP-1
#workspace = 8, monitor:eDP-1
#workspace = 9, monitor:eDP-1
#workspace = 0, monitor:eDP-1

# 其他自定义配置, 请自行创建 custom.conf 文件，然后解除注释
#source = ~/.config/hypr/custom.conf
